import { FC } from 'react';
import styles from './Contacts.module.scss';

export const Contacts: FC = () => {
  return (
    <div>
      <div className={styles.caption}>Контакты</div>
    </div>
  );
};

export default Contacts;
