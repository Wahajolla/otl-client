import { createAsyncThunk } from '@reduxjs/toolkit';

export {};

// export const searchProductThunk = createAsyncThunk<
//   void,
//   Product,
//   { state: RootState }
// >('product/searchProduct', async (product: Product, { dispatch, getState }) => {
//   dispatch(removeOneItem(product));
//   dispatch(incVersion());
//   syncCart(dispatch as AppDispatch, getState());
// });
